cdns:
# Multiple cdn instance limited by port collision on the same VPC
# A single VPC can host a single origin or pop location on the same port numbers
- instance: ffff  # id+ff
  origin: # Origin server
    ip:
    port:  # port exposed on the VPC / public IP
  pop:
    - vpcid:
    - vpcid:
    - vpcid:
    - vpcid:
